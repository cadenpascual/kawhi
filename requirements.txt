# Core Data Science & Math
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Wasserstein Gradient Flow & Optimization
jax>=0.4.20
jaxlib>=0.4.20
optax>=0.1.7
ott-jax>=0.4.0    # Required for Sinkhorn Divergence in the JKO scheme
optuna>=3.4.0     # Required for Bayesian hyperparameter optimization

# Visualizations
matplotlib>=3.8.0
seaborn>=0.13.0   # Required for statistical data visualization (KDEs, distributions)
plotly>=6.1.1    # Required for interactive court animations
kaleido>=0.2.1    # Required for exporting Plotly charts and Pareto fronts
Pillow>=10.0.0    # Required for generating GIF files

# Data I/O & Parsing
orjson>=3.9.0
py7zr>=0.20.0     # Required for extracting raw NBA tracking archives
pyarrow>=14.0.0   # Required for Pandas to read/write .parquet files

# APIs, Notebooks & Utils
nba_api>=1.4.1
jupyter>=1.0.0
tqdm>=4.66.0      # Required for simulation progress bars